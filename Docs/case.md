## Log out 
    1. when logout time teh session not cleared access_token ,user, userInfo there are multiple key user, userInfo . keep one !! clear it 
## Login 
    1. Password incorrect show error unexpected error occure 

## Quotation Ai
    1. defaultly load the customer name 
    2. tune the prompt (only take data from inventory !!)
    3. when ai not work raw input directly move to quotation 
    4.


## invoice 
    1. when quotation all amount will be 0 then , convert to quotation it will be NaN
    done :::::: 2. the quotation save click , then quotation is already convert to invoice then  show print update buttons normally !! . no need save button 
    case :
        step1 -  when wuoattion save and convert to invoice and save invoice 
        step2 - direct click on newquot in sidebar can see latest quotation then can see convert to invoice button 
        then click save button can see this is major case !!
        back end will not approve for new invoice but front end can see alert only the button still in a new quotation !! 